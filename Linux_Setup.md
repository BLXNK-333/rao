# 🧰 Инструкция (Python 3.12 + venv + tkinter)

Если коротко:

```bash
sudo dnf install python3.12 python3.12-devel python3.12-tkinter

python3.12 -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
```